# Cortex Configuration

# Server port
PORT=3001

# Projects directory (where state files are located)
# Windows example: C:\Users\<username>\Documents\_Projects
# macOS/Linux example: /home/<username>/Projects
PROJECTS_DIR=/path/to/your/projects

# Obsidian vault directory (primary — required)
# Windows example: C:\Users\<username>\Documents\SecondBrain
# macOS/Linux example: /home/<username>/SecondBrain
VAULT_DIR=/path/to/your/vault

# Additional vault directories (optional, comma-separated absolute paths)
# TODOs and deadlines will be scanned from all configured vaults.
# Non-existent paths are skipped with a warning log.
# Example: VAULT_DIRS=C:\Users\<username>\Notes\AppleNotes,D:\Logseq
VAULT_DIRS=

# Claude API key (optional, for AI features)
ANTHROPIC_API_KEY=

# Notes corpus file (optional)
# Example: C:\Users\<username>\Documents\notes_corpus.txt
NOTES_CORPUS=/path/to/notes_corpus.txt

# ntfy push notifications (https://ntfy.sh)
NTFY_TOPIC=
NTFY_DIGEST_TIME=08:00
NTFY_URL=https://ntfy.sh

# Ollama local AI (https://ollama.ai)
OLLAMA_URL=http://localhost:11434
OLLAMA_MODEL=llama3.1:8b

# Google Calendar OAuth2 (optional)
# See README.md "Google Calendar Setup" section for instructions
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URI=http://localhost:3001/api/calendar/callback

# YouTube watch history (Google Takeout)
# Path to watch-history.json from Google Takeout export
# See VAULT_DIR/Resources/YouTube-Takeout-Setup.md for instructions
YOUTUBE_HISTORY_PATH=

# Chrome Bookmarks (optional)
# Path to Chrome Bookmarks file. Defaults to %LOCALAPPDATA%\Google\Chrome\User Data\Default\Bookmarks
# Only needed if you want to override the default location
CHROME_BOOKMARKS_PATH=
